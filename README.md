# Albion Online Market Screen Reader

Программа для записи данных с рынка (аукциона)

![Albion Online Market Screen Reader](./preview.png)

---

## ⚠️ Важные предупреждения

### 1. Назначение программы

**Эта программа НЕ управляет игрой и НЕ автоматизирует игровой процесс.**

Программа предназначена **только для автоматизации сбора данных** с рынка в **ручном режиме**:

- Вы **сами** открываете рынок в игре
- Вы **сами** листаете предметы
- Вы **нажимаете горячую клавишу** для записи данных
- Программа **только сохраняет** данные в CSV-файл, чтобы вы не вводили их вручную

**Программа не:**

- ❌ Не кликает за вас
- ❌ Не перемещает персонажа
- ❌ Не взаимодействует с интерфейсом игры
- ❌ Не играет в игру

### 2. Риск использования

**Используйте на свой страх и риск!**

Хотя программа только читает данные с экрана (не внедряется в игру), некоторые античиты могут реагировать на:

- Перехват клавиатуры (библиотека `keyboard`)
- **Перехват мыши (категорически не рекомендуется!)**
- Скриншоты экрана

**Рекомендации по безопасности:**

- ✅ Используйте только клавиатурные горячие клавиши (PrintScreen, F1-F12)
- ❌ **НЕ используйте кнопки мыши** — это может быть расценено как макрос/бот
- ✅ Делайте скриншоты только в ручном режиме
- ❌ Не оставляйте программу работать в автоматическом режиме

### 3. Почему эта программа создана?

**Проблема:** Данные на сторонних сайтах (albion-online-data.com, albion2d.com и др.) часто **неактуальны** из-за задержек обновления.

**Решение:** Эта программа позволяет **мгновенно получать актуальные цены** прямо с вашего экрана, без ожидания обновления сайтов.

**Вы получаете:**

- ✅ Цены в реальном времени
- ✅ Данные с вашего сервера и региона
- ✅ Полный контроль над сбором данных
- ✅ Локальное хранение (никаких внешних серверов)

---

## Описание

При нажатии клавиши **Print Screen** программа:

1. Делает снимок основного монитора
2. Сохраняет изображение в папку `screenshots`
3. Анализирует содержимое с помощью компьютерного зрения
4. Извлекает информацию о товаре и ценах
5. Сохраняет данные в CSV-файл

---

## Быстрый старт

1. **Установите зависимости:**

Загружаем в корень проекта скриншот, например `example.png`.

> С его помощью мы будем обучать программу считывать нужные данные с учетом вашего экрана.

Туда же загружаем файл [item.js](https://github.com/ao-data/ao-bin-dumps/blob/master/formatted/items.json), его можно взять с официального сайта.

2. **Установите зависимости:**

   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте файл конфигурации:**
   - Скопируйте `.env.example` в `.env`:
     ```bash
     cp .env.example .env
     ```
   - Откройте `.env` и настройте параметры (путь к Tesseract, горячая клавиша и т.д.)

4. **Запустите программу:**

   ```bash
   python main.py
   ```

Откроется интерактивное меню с выбором действий.

---

## Интерактивное меню

При запуске `main.py` открывается главное меню:

```
╔═══════════════════════════════════════════════╗
║    Albion Online Market Screen Reader         ║
╠═══════════════════════════════════════════════╣
║  → Обработка справочника (item.json)          ║
║    Калибровка изображения (example.png)       ║
║    Установка горячей клавиши                  ║
║    Мониторинг экрана                          ║
║    Выход                                      ║
╚═══════════════════════════════════════════════╝

Стрелки: выбор | Enter: начать | Esc: выход
```

### Пункты меню:

1. **Обработка справочника (item.json)** — запуск скрипта обработки каталога предметов
2. **Калибровка изображения (example.png)** — интерактивная настройка зон распознавания
3. **Установка горячей клавиши** — выбор клавиши или комбинации для захвата скриншота
4. **Мониторинг экрана** — запуск основной программы (ожидание нажатия горячей клавиши)
5. **Выход** — завершение работы программы

---

## Конфигурация

### Файл .env

Программа использует файл `.env` для настройки параметров:

```env
# Режим отладки (true/false)
DEBUG_MODE=false

# Горячая клавиша для захвата скриншота
HOTKEY=print_screen

# Путь к Tesseract OCR
TESSERACT_PATH=C:\Users\USERNAME\AppData\Local\Programs\Tesseract-OCR\tesseract.exe

# Язык OCR
OCR_LANG=rus+eng

# Папка для скриншотов
SCREENSHOTS_DIR=screenshots

# Файл выходных данных
DATA_FILE=market_data.csv

# Файл конфигурации ROI
ROI_CONFIG_FILE=roi_config.json

# Путь к items.json каталогу
ITEMS_CATALOG_PATH=items.json

# Файл примера для калибровки
EXAMPLE_FILE=example.png
```

---

## Логирование

Программа использует централизованное логирование с ротацией по времени:

- **Папка логов:** `logs/`
- **Файл логов:** `logs/app.log`
- **Ротация:** ежедневно в полночь
- **Хранение:** 30 дней

**Формат записи:**

```
2026-06-11 17:30:00 - src.main - INFO - Запуск Albion Online Market Screen Reader...
```

---

## Структура проекта

```
project_root/
├── main.py                # Точка входа (интерактивное меню)
├── .env                   # Конфигурационный файл (не в git)
├── .env.example           # Пример конфигурации
├── README.md              # Документация
├── requirements.txt       # Зависимости Python
├── roi_config.json        # Конфигурация зон (генерируется калибровкой)
├── items.json             # Справочник предметов (с тир и зачарованием)
├── market_data.csv        # Файл с накопленными данными (CSV формат)
├── example.png            # Пример скриншота для изучения блоков
├── logs/                  # Папка для логов
│   └── app.log            # Текущий лог + ротированные файлы
├── scripts/               # Скрипты для обработки данных
│   ├── process_items_catalog.py  # Обработка items.json
│   └── calibration_tool.py       # Инструмент калибровки зон
├── screenshots/           # Папка для сохранённых скриншотов
│   └── YYYY-MM-DD-HH-MM-SS.png
├── src/                   # Исходный код
│   ├── __init__.py
│   ├── config.py          # Конфигурация и константы
│   ├── logging_config.py  # Централизованное логирование
│   ├── models.py          # Модели данных
│   ├── services/          # Сервисы (business logic)
│   │   ├── __init__.py
│   │   ├── screenshot_service.py
│   │   ├── image_analysis_service.py
│   │   ├── data_storage_service.py
│   │   └── items_catalog_service.py
│   └── utils/             # Утилиты
│       ├── __init__.py
│       └── hotkey_listener.py
└── tests/                 # Тесты (опционально)
    └── __init__.py
```

---

## Форматы данных

### Имя файла скриншота

```
YYYY-MM-DD-HH-MM-SS.png
```

**Пример:** `2025-01-15-14-30-45.png`

### Структура данных в market_data.csv

```csv
screenshot_date;city;item_name;item_tier;item_enchantment;item_quality;sell_price;buy_price;average_price
2025-01-15T14:30:45;Martlock;"Лен";3;0;0;50;55;52
```

| Поле               | Описание                                             |
| ------------------ | ---------------------------------------------------- |
| `screenshot_date`  | Дата и время создания скриншота (ISO 8601)           |
| `city`             | Город рынка (английское название, например Martlock) |
| `item_name`        | Название предмета (распознаётся рядом с иконкой)     |
| `item_tier`        | Тир предмета (0-8), извлекается из справочника       |
| `item_enchantment` | Зачарование предмета (0-4), из справочника           |
| `item_quality`     | Качество предмета (временно 0)                       |
| `sell_price`       | Цена продажи (в правой части экрана)                 |
| `buy_price`        | Цена покупки (в правой части экрана)                 |
| `average_price`    | Средняя цена (в правой части экрана)                 |

---

## Блоки для распознавания (по example.png)

Пример файла `example.png` содержит размеченные области для обучения и настройки:

| Цвет блока        | Что содержит      | Язык       |
| ----------------- | ----------------- | ---------- |
| 🟠 **Оранжевый**  | Город рынка       | Английский |
| 🔵 **Синий**      | Название предмета | Русский    |
| 🔴 **Красный**    | Цена покупки      | Числа      |
| 🟢 **Зелёный**    | Цена продажи      | Числа      |
| 🟣 **Фиолетовый** | Средняя цена      | Числа      |

Эти зоны используются для калибровки алгоритма компьютерного зрения и настройки областей интереса (ROI — Region of Interest).

> **Важно:** Все 5 зон обязательны для заполнения!

---

## Логика работы

1. **Отслеживание хоткея**  
   Программа работает в фоновом режиме и перехватывает нажатие **Print Screen** на глобальном уровне.

2. **Захват экрана**  
   При срабатывании триггера делается снимок **первого (основного) монитора**.

3. **Сохранение изображения**  
   Файл сохраняется в папку `screenshots/` с именем в формате `YYYY-MM-DD-HH-MM-SS.png`.

4. **Анализ изображения**
   - Загружается скриншот
   - Определяются цветные блоки (или их текстовые аналоги на реальном интерфейсе)
   - С помощью **OCR** распознаётся текст в каждой зоне
   - Данные структурируются и приводятся к нужному формату

5. **Обогащение данными из справочника**
   - По названию предмета выполняется поиск в `items.json`
   - Извлекаются: тир (tier), зачарование (enchantment), качество (quality)
   - Если предмет не найден, используется fallback-парсинг из названия

6. **Сохранение в CSV**
   - Загружается существующий файл `market_data.csv` (если есть)
   - Новая запись с обогащёнными данными добавляется в таблицу
   - Файл сохраняется обратно в корень проекта

---

## Технологический стек

| Компонент             | Библиотека              | Назначение                        |
| --------------------- | ----------------------- | --------------------------------- |
| Перехват клавиш       | `keyboard`              | Глобальные хоткеи (фоновый режим) |
| Захват экрана         | `mss`                   | Быстрый снимок основного монитора |
| Обработка изображений | `opencv-python`         | Анализ и обработка изображений    |
| Распознавание текста  | `pytesseract`           | OCR-движок (поддержка кириллицы)  |
| Работа с данными      | `stdlib` (csv, pathlib) | CSV-манипуляции, файловая система |
| Справочник предметов  | `stdlib` (json, re)     | Парсинг и обогащение данных       |

## Инструменты

| Инструмент            | Назначение                                                 |
| --------------------- | ---------------------------------------------------------- |
| `calibration_tool.py` | Интерактивная настройка зон распознавания                  |
| `roi_config.json`     | Файл с координатами зон (генерируется calibration_tool.py) |

---

## Примеры использования

### Настройка Tesseract OCR

Если Tesseract установлен не по умолчанию, настройте путь в `.env`:

```env
TESSERACT_PATH=C:\Users\USERNAME\AppData\Local\Programs\Tesseract-OCR\tesseract.exe
```

**Проверка поддержки кириллицы:**

```bash
tesseract --list-langs
```

Если `rus` отсутствует, скачайте `rus.traineddata` с https://github.com/tesseract-ocr/tessdata и поместите в папку `tessdata` внутри установки Tesseract.

### Калибровка зон распознавания

1. В главном меню выберите пункт **2. Калибровка изображения (example.png)**
2. Откроется окно с изображением `example.png`
3. Последовательно выделите **5 зон**:
   - 🟠 **city** — город рынка (английское название, например "Martlock")
   - 🔵 **title** — название предмета
   - 🔴 **buy_price** — цена покупки
   - 🟢 **sell_price** — цена продажи
   - 🟣 **avg_price** — средняя цена
4. Координаты сохранятся в `roi_config.json`
5. Нажмите `q` для выхода, `u` для отмены выделения

> **Важно:** Все 5 зон обязательны! Город должен быть выделен на английском языке.

### Установка горячей клавиши

1. В главном меню выберите пункт **3. Установка горячей клавиши**
2. Нажмите желаемую клавишу или комбинацию
3. Горячая клавиша сохранится в `.env`

**Примеры значений:**

- `print_screen` — клавиша PrintScreen (рекомендуется)
- `f1` — функциональная клавиша
- `alt+enter` — комбинация клавиш

> ⚠️ **ВАЖНО:** Используйте **только клавиатурные клавиши**!  
> **Кнопки мыши не поддерживаются** из-за риска получить блокировку аккаунта.  
> Перехват мыши может быть расценён античитом как макрос или бот.

### Запуск мониторинга

1. В главном меню выберите пункт **4. Мониторинг экрана**
2. Программа работает в фоновом режиме
3. Нажмите настроенную горячую клавишу для захвата скриншота
4. Для остановки нажмите `Ctrl+C`

После остановки папка `screenshots/` автоматически очищается.

### Обработка справочника предметов

1. В главном меню выберите пункт **1. Обработка справочника (item.json)**
2. Скрипт обработает `items.json`:
   - Оставит только русские и английские названия
   - Извлечёт тир и зачарование из `UniqueName`
   - Сохранит обработанный файл

---

### Отладка

При включённом `DEBUG_MODE=true` в `.env`:

- В папке `screenshots/` сохраняется `debug_YYYY-MM-DD-HH-MM-SS.png` с наложенными зонами
- В логах отображается распознанный текст из каждой зоны

---

### Просмотр данных

Откройте `market_data.csv` в любом текстовом редакторе или в Excel:

```bash
type market_data.csv
```

---

### Просмотр логов

Логи находятся в папке `logs/`:

```bash
type logs\app.log
```

Или откройте файл в любом текстовом редакторе.

---

## Требования к окружению

- Python 3.10+
- Tesseract OCR с поддержкой русского языка
- Доступ к файловой системе (корень проекта и папка `screenshots/`)
- Разрешения на захват экрана (в некоторых ОС)
- Библиотеки из `requirements.txt`

---

## Примечания

- Все скриншоты хранятся локально, данные не отправляются на внешние сервера
- При удалении файла `market_data.csv` история данных будет утеряна
- Рекомендуется периодически делать резервные копии CSV-файла
- Файл `example.png` используется как референс для настройки зон распознавания
- Логи автоматически ротируются и хранятся 30 дней
- **Программа не нарушает правила игры** — она только читает данные с экрана
- **Используйте только клавиатурные хоткеи** — мышь может вызвать подозрения античита

---

## Отказ от ответственности

Эта программа предоставляется «как есть», без каких-либо гарантий. Автор не несёт ответственности за:

- Любые последствия использования программы (включая блокировку аккаунта)
- Неправильную работу программы в сочетании с другим ПО
- Изменения в игре, которые могут нарушить работу программы

**Используя эту программу, вы принимаете на себя все риски.**

Программа создана исключительно в образовательных целях и для демонстрации возможностей компьютерного зрения и OCR.

---

## Реализованные улучшения

- [x] Калибровка зон распознавания через GUI (calibration_tool.py)
- [x] Отладочный режим с визуализацией зон
- [x] Перевод названий на русский язык (кириллица вместо латиницы)
- [x] Добавление справочника `items.json` по всем предметам (с тир и зачарованием)
- [x] Конфигурация через файл `.env`
- [x] Единая точка входа с интерактивным меню
- [x] Централизованное логирование с ротацией по времени
- [x] Установка горячей клавиши через меню
- [x] Автоматическая очистка папки screenshots после мониторинга
- [x] CSV-файл в формате Excel (UTF-8 с BOM, разделитель `;`)
- [x] Обработка предупреждений Tesseract без прерывания работы
- [x] Распознавание города рынка (оранжевая зона, английский язык)
- [x] Новый порядок столбцов CSV: дата, город, предмет, тир, зачарование, качество, цены
